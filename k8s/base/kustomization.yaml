apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: shark-mqtt

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  managed-by: kustomize
