# Shark-MQTT 代码审查报告与改进计划

**日期**: 2026-05-06 12:25:06
**审查角色**: 服务端开发 / MQTT 协议 / 分布式消息系统审查
**审查范围**: 全项目源码、测试、脚本、部署与说明文档
**Go 版本声明**: `go.mod` 使用 Go 1.26.1
**协议范围**: MQTT 3.1.1 / 5.0

---

## 审查与测试执行

本轮重新审查项目当前状态，覆盖根目录、`api/`、`broker/`、`client/`、`config/`、`errs/`、`pkg/`、`plugin/`、`protocol/`、`store/`、`tests/`、`testutils/`、`scripts/`、`deploy/`、`docs/`、`examples/` 等目录下的文本源码、配置和文档文件。

已执行测试：

| 命令 | 结果 | 日志 |
|------|------|------|
| `go run scripts/run_tests.go -mode all` | 通过 | `logs/20260506_121959_unit.*`, `logs/20260506_121959_integration.*`, `logs/20260506_121959_benchmark.*` |
| `go test ./... -count=1 -timeout=300s` | 通过 | 终端输出 |
| `go vet ./...` | 通过 | 终端输出 |

全量脚本结果：

- 单元测试：通过
- `tests/integration` 自动化测试：通过，79 passed
- 基准测试：通过，67 benchmarks
- `go vet`：无发现

---

## 本轮发现缺陷

### FIX-20260506-1: Retained message 指标计数覆盖/删除时漂移

**位置**: `broker/broker.go`
**严重度**: 中
**类型**: 可观测性 / 运维指标正确性

`Broker.handlePublish` 在处理 retained message 时，仅根据写入或删除动作对 `retainedCount` 做 `+1/-1`：

- 同一 topic 覆盖 retained message 会重复 `+1`，指标高于真实 retained topic 数。
- 删除不存在的 retained topic 会 `-1`，存在计数变负风险。
- 并发覆盖同一 topic 时，读写与计数更新没有统一临界区，仍可能漂移。

**影响**: Prometheus `retained_messages` gauge 不能反映真实 retained message 数量，容量告警、运行状态判断和问题排查会被误导。

**确认测试**:

- 新增 `TestBroker_RetainedMetricsCountUpdatesOnOverwriteAndDelete`
- 修复前结果：`retained metric updates = [1 2 1 0], want [1 1 0 0]`

**修复计划**:

1. 在 retained 写入/删除前检查 topic 是否已存在。
2. 将存在性检查、store 写入/删除、计数更新置于 broker 级 retained 临界区。
3. 覆盖时保持计数不变，删除不存在 topic 时保持计数不变。
4. 用定向测试和全量测试验证。

---

## 改进执行状态

| ID | 状态 | 验证 |
|----|------|------|
| FIX-20260506-1 | 已修复，待提交 | `go test ./broker -run TestBroker_RetainedMetricsCountUpdatesOnOverwriteAndDelete -count=1 -v` 通过 |

---

## 后续复核重点

- 文档中的测试数量、基准数量需要与当前日志同步。
- README、`docs/testing.md`、`docs/performance.md`、`docs/PROJECT_STATUS.md` 需要补充本轮 retained metrics 修复记录。
- 继续关注 MQTT 5.0 未实现能力的文档边界，避免把未实现特性描述为完整支持。
