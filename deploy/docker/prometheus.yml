global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "shark-mqtt"
    static_configs:
      - targets: ["shark-mqtt:9090"]
    metrics_path: /metrics
    scheme: http
